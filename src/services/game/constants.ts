import { GameScore } from "./get-matrix"

export const INITIAL_SCORE: GameScore = [0, 0]
export const TIME_ON_MOVE_SECONDS = 30
export const ROWS = 6
export const COLS = 7
export const allNodesCount = ROWS * COLS
